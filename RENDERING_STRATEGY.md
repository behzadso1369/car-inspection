# استراتژی رندرینگ پروژه کارشناسی خودرو (Car Inspection)

این سند توضیح می‌دهد که چرا هر صفحه از پروژه از یک نوع خاص رندرینگ استفاده می‌کند.

## 📚 انواع رندرینگ در Next.js

### 1. SSG (Static Site Generation) - تولید استاتیک
- صفحات در زمان build ساخته می‌شوند
- سریع‌ترین روش برای کاربر
- مناسب برای محتوای ثابت
- بهترین SEO

### 2. ISR (Incremental Static Regeneration) - تولید استاتیک تدریجی
- صفحات استاتیک با قابلیت به‌روزرسانی دوره‌ای
- ترکیبی از سرعت SSG و داده‌های تازه
- مناسب برای محتوایی که گاهی آپدیت می‌شود
- SEO عالی + داده‌های به‌روز

### 3. SSR (Server-Side Rendering) - رندرینگ سمت سرور
- صفحات در هر request ساخته می‌شوند
- داده‌های همیشه تازه
- مناسب برای محتوای دینامیک و شخصی‌سازی شده
- SEO خوب

### 4. CSR (Client-Side Rendering) - رندرینگ سمت کلاینت
- صفحات در مرورگر کاربر ساخته می‌شوند
- مناسب برای صفحات تعاملی و خصوصی
- نیازی به SEO ندارند
- تجربه کاربری روان برای اپلیکیشن‌ها

---

## 🎯 استراتژی پیاده‌سازی شده

### ✅ صفحات SSG (Static Site Generation)

#### 1. `/contact-us` - تماس با ما
**چرا SSG؟**
- اطلاعات تماس ثابت هستند (شماره تلفن، آدرس)
- تغییر نادر (ماه‌ها یک بار)
- نیاز به سرعت بالا برای نمایش
- SEO مهم است

**مزایا:**
- بارگذاری فوری صفحه
- هزینه سرور صفر
- SEO عالی

---

### 🔄 صفحات ISR (Incremental Static Regeneration)

#### 1. `/` - صفحه اصلی (Homepage)
**revalidate: 600 ثانیه (10 دقیقه)**

**چرا ISR؟**
- محتوای دینامیک (اسلایدرها، بلاگ‌های جدید)
- نیاز به سرعت بالا
- محتوا هر چند ساعت یکبار تغییر می‌کند
- SEO بسیار مهم (صفحه ورودی)

**مزایا:**
- سرعت بالای SSG
- محتوای به‌روز بدون نیاز به build مجدد
- تجربه کاربری سریع برای بازدیدکنندگان

#### 2. `/services` - سرویس‌ها
**revalidate: 3600 ثانیه (1 ساعت)**

**چرا ISR؟**
- لیست سرویس‌ها و قیمت‌ها نسبتاً ثابت است
- تغییرات ماهانه یا فصلی
- SEO مهم برای جذب مشتری
- نیاز به نمایش قیمت‌های به‌روز

**مزایا:**
- کاربران همیشه قیمت‌های صحیح را می‌بینند
- بدون تاخیر در بارگذاری
- امکان تغییر سرویس‌ها بدون deploy

#### 3. `/about-us` - درباره ما
**revalidate: 3600 ثانیه (1 ساعت)**

**چرا ISR؟**
- محتوای نسبتاً ثابت
- ممکن است آمار و اطلاعات آپدیت شوند
- SEO مهم برای اعتماد‌سازی
- نیاز به نمایش آمار به‌روز (تعداد کارشناسی‌ها)

**مزایا:**
- نمایش آمار واقعی بدون کندی
- SEO قوی برای برندینگ

#### 4. `/regulations` - قوانین و مقررات
**revalidate: 3600 ثانیه (1 ساعت)**

**چرا ISR؟**
- قوانین ممکن است تغییر کنند (نادر)
- نیاز به نمایش آخرین ویرایش
- SEO مهم برای شفافیت
- محتوای طولانی که باید سریع بارگذاری شود

**مزایا:**
- همیشه آخرین نسخه قوانین نمایش داده می‌شود
- بدون نیاز به deploy برای تغییر قوانین

#### 5. `/blog/[id]` - جزئیات بلاگ
**revalidate: 1800 ثانیه (30 دقیقه)**

**چرا ISR با generateStaticParams؟**
- مقالات پس از انتشار کمتر ویرایش می‌شوند
- SEO بسیار مهم (ترافیک ارگانیک)
- نیاز به سرعت بالا
- pre-render کردن 10 مقاله محبوب

**مزایا:**
- مقالات محبوب با سرعت بالا لود می‌شوند
- SEO عالی برای هر مقاله
- امکان ویرایش مقالات بدون deploy

**پیاده‌سازی:**
```typescript
export const revalidate = 1800;

export async function generateStaticParams() {
  // 10 مقاله محبوب را pre-render می‌کنیم
  const categories = await fetchCategories();
  return categories.slice(0, 10).map(cat => ({
    id: String(cat.Id)
  }));
}
```

---

### 🎨 صفحات CSR (Client-Side Rendering)

#### 1. صفحات احراز هویت
- `/login` - ورود
- `/register` - ثبت‌نام  
- `/verify-otp` - تایید کد

**چرا CSR؟**
- نیاز به localStorage برای مدیریت session
- فرم‌های تعاملی با validation
- مدیریت timer (در verify-otp)
- صفحات خصوصی - نیازی به SEO ندارند
- حساس به امنیت

**ویژگی‌ها:**
- مدیریت وضعیت (state) پیچیده
- تعامل زیاد با کاربر
- ذخیره token و cookie

#### 2. جریان کارشناسی `/car-inspection-flow/*`
تمام مراحل:
- `/select-car-group` - انتخاب خودرو
- `/inspection-method` - روش کارشناسی
- `/inspection-location` - محل کارشناسی
- `/insert-information` - وارد کردن اطلاعات
- `/inspection-time` - زمان کارشناسی
- `/final-confirm` - تایید نهایی
- `/payment-success` - پرداخت موفق

**چرا CSR؟**
- فرآیند چند مرحله‌ای با وابستگی به هم
- نیاز به localStorage برای نگهداری وضعیت
- انیمیشن‌ها و تعاملات پیچیده
- مدیریت state پیچیده بین صفحات
- صفحات خصوصی - نیازی به SEO ندارند

**معماری:**
```
مرحله 1 → ذخیره در localStorage → مرحله 2 → ... → پرداخت
```

#### 3. پروفایل کاربری `/Profile/*`
- `/Profile` - صفحه اصلی پروفایل
- `/Profile/requests` - درخواست‌ها
- `/Profile/requests/[id]` - جزئیات درخواست

**چرا CSR؟**
- نیاز به احراز هویت (token)
- داده‌های شخصی کاربر
- نباید pre-render شوند (امنیت)
- تعاملات زیاد (ویرایش، حذف، logout)
- نیازی به SEO ندارند (محتوای خصوصی)

#### 4. `/blog` - لیست بلاگ‌ها
**چرا CSR؟**
- جستجوی لحظه‌ای (search)
- تغییر دسته‌بندی‌ها (tabs)
- Carousel برای نمایش دسته‌ها
- فیلتر و sort کردن
- state management پیچیده

**ویژگی‌ها:**
- Search box با نتایج آنی
- Tab switching بدون reload
- Infinite scroll (قابل اضافه شدن)

#### 5. `/faq` - سوالات متداول
**چرا CSR؟**
- جستجو در سوالات
- تغییر دسته‌بندی‌ها
- Accordion برای سوالات
- فیلتر دینامیک
- تعامل زیاد با کاربر

---

## 📊 جدول خلاصه استراتژی

| صفحه | نوع رندرینگ | Revalidate | دلیل اصلی |
|------|-------------|-----------|-----------|
| `/` | ISR | 10 دقیقه | SEO + محتوای دینامیک |
| `/about-us` | ISR | 1 ساعت | SEO + آمار متغیر |
| `/services` | ISR | 1 ساعت | SEO + قیمت‌های متغیر |
| `/contact-us` | SSG | - | محتوای ثابت |
| `/regulations` | ISR | 1 ساعت | قوانین نادر تغییر می‌کنند |
| `/blog` | CSR | - | تعاملات زیاد |
| `/blog/[id]` | ISR | 30 دقیقه | SEO + محتوای نسبتاً ثابت |
| `/faq` | CSR | - | search + accordion |
| `/login` | CSR | - | authentication |
| `/register` | CSR | - | form handling |
| `/verify-otp` | CSR | - | timer + validation |
| `/Profile/*` | CSR | - | داده‌های خصوصی |
| `/car-inspection-flow/*` | CSR | - | فرآیند چندمرحله‌ای |

---

## 🎯 مزایای این استراتژی

### 1. **بهبود SEO** 📈
- صفحات مهم (home, services, about) با ISR/SSG برای SEO بهینه شده‌اند
- محتوای کامل در اولین بارگذاری موجود است
- Meta tags و structured data در سرور generate می‌شوند

### 2. **سرعت بالا** ⚡
- صفحات استاتیک از CDN سرو می‌شوند
- کاربران محتوا را فوری دریافت می‌کنند
- Time to First Byte (TTFB) بسیار کم

### 3. **کاهش هزینه سرور** 💰
- صفحات استاتیک فشار کمی به سرور وارد می‌کنند
- فقط هر N دقیقه یکبار صفحه regenerate می‌شود
- مقیاس‌پذیری بالا

### 4. **تجربه کاربری عالی** 🎨
- صفحات تعاملی روان و responsive
- انیمیشن‌ها و transitions نرم
- state management بهینه

### 5. **امنیت بالاتر** 🔒
- صفحات خصوصی در client render می‌شوند
- token‌ها در localStorage نگهداری می‌شوند
- داده‌های حساس pre-render نمی‌شوند

---

## 🔧 نکات پیاده‌سازی

### تنظیم متغیر محیطی
```env
NEXT_PUBLIC_API_BASE_URL=https://api.carmacheck.com
```

### مثال fetch در ISR
```typescript
export const revalidate = 3600; // 1 hour

async function getData() {
  const response = await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL}/api/endpoint`, {
    next: { revalidate: 3600 }
  });
  return response.json();
}
```

### مثال generateStaticParams
```typescript
export async function generateStaticParams() {
  const items = await fetchItems();
  
  return items.map((item) => ({
    id: String(item.Id),
  }));
}
```

---

## 🚀 بهینه‌سازی‌های آینده

1. **Edge Caching**: استفاده از Vercel Edge Network برای سرعت بیشتر
2. **Partial Prerendering**: ترکیب SSR و streaming برای صفحات خاص
3. **React Server Components**: استفاده از RSC برای کامپوننت‌های server-side
4. **Suspense Boundaries**: بهبود تجربه loading
5. **Image Optimization**: استفاده بهینه از next/image

---

## 📝 نتیجه‌گیری

این استراتژی ترکیبی باعث می‌شود:
- ✅ صفحات عمومی سریع و SEO-friendly باشند (ISR/SSG)
- ✅ صفحات تعاملی تجربه کاربری روانی داشته باشند (CSR)
- ✅ هزینه سرور به حداقل برسد
- ✅ کاربران همیشه محتوای تازه و به‌روز ببینند
- ✅ امنیت داده‌های حساس حفظ شود

---

**تاریخ ایجاد**: دسامبر 2025  
**نسخه**: 1.0  
**پروژه**: کارشناسی خودرو کارچک

